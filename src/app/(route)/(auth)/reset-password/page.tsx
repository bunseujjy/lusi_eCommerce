import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reset Password",
  description: "Reset Password to continue to purchase product in this website",
};

const page = () => {
  return <div>page</div>;
};

export default page;
